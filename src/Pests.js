import {Navigation} from './Navigation';

const Pests = ({setScreen}) => (
  <div className="Pests">
    <h1>Economic Decision Aid for Cover Crops: Pests</h1>
    <Navigation setScreen={setScreen} current={Pests} />
  </div>
) // Pests

export default Pests;