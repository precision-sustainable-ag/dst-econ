import {Navigation} from './Navigation';

const Termination = ({set}) => (
  <div className="Termination">
    <h1>Economic Decision Aid for Cover Crops: Termination</h1>
    <Navigation set={set} current={Termination} />
  </div>
) // Termination

export default Termination;